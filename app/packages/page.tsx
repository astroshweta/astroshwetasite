import Layout from "../layout";
import { Typography } from "@mui/material";

export default function Packages() {
  return (
    <Layout>
      <Typography variant="h4">Our Packages</Typography>
      <Typography>Select a package that suits your needs.</Typography>
    </Layout>
  );
}
